"use client";
import { useEffect,useState } from "react";
export default function Page(){const [settings,setSettings]=useState<Record<string,string>>({});useEffect(()=>{const timezone=Intl.DateTimeFormat().resolvedOptions().timeZone;fetch(`/api/catalog?timezone=${encodeURIComponent(timezone)}`).then(r=>r.json()).then(d=>setSettings(d.settings||{})).catch(()=>{});},[]);const phone=settings.phone||"";const address=settings.address||"";return <main className="category-page"><header className="category-header wrap"><a href="/" className="category-brand"><img src="/envol-logo-officiel.svg" alt="Envol des Enfants"/></a><a href="/" className="category-back">← Accueil</a></header><section className="category-hero wrap"><p className="eyebrow">Envol des Enfants</p><h1>Nous trouver</h1><p>{address||"Coordonnées de la boutique."}</p>{phone&&<p><a href={`tel:${phone.replace(/\s/g,"")}`}><strong>{phone}</strong></a></p>}{settings.opening_hours&&<p>{settings.opening_hours}</p>}</section></main>}
