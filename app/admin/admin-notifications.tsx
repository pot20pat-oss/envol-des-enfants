"use client";

import { useMemo, useState } from "react";
import { marketPrice, markets, type Market } from "@/lib/markets";
import type { Row } from "./admin-shared";

type Problem={product:Row;issues:string[];severity:"high"|"medium"};

function text(value:unknown){return String(value||"").trim()}

export function NotificationsSection({products,market,onEdit,reload}:{products:Row[];market:Market;onEdit:(product:Row)=>void;reload:()=>Promise<void>}){
 const[aiBusy,setAiBusy]=useState<string|null>(null);const[aiNotice,setAiNotice]=useState("");const[selected,setSelected]=useState<Set<string>>(new Set());const[batchBusy,setBatchBusy]=useState(false);const[batchAction,setBatchAction]=useState<"ai"|"hide"|"qc"|"conakry"|"both"|"price_set"|"price_percent"|"stock_set"|"stock_add">("ai");const[batchValue,setBatchValue]=useState("");const[batchProgress,setBatchProgress]=useState({done:0,total:0});
 const[filter,setFilter]=useState<"all"|"price"|"description"|"image"|"stock"|"name"|"pending">("all");
 const[search,setSearch]=useState("");const[brandFilter,setBrandFilter]=useState("all");const[categoryFilter,setCategoryFilter]=useState("all");const[visibilityFilter,setVisibilityFilter]=useState<"all"|"visible"|"hidden">("all");
 async function repairWithAi(product:Row){
  const id=String(product.id||"");if(!id||!text(product.image_url))return;
  setAiBusy(id);setAiNotice("");
  try{
   const analyzed=await fetch("/api/admin/analyze-product",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({image_url:String(product.image_url)})});
   const payload=await analyzed.json() as {suggestion?:Record<string,unknown>;error?:string};
   if(!analyzed.ok||!payload.suggestion)throw new Error(payload.error||"Analyse IA impossible.");
   const s=payload.suggestion;
   const patched={...product,
    name_fr:text(product.name_fr)||text(s.name_fr),
    name_en:text(product.name_en)||text(s.name_en),
    description_fr:text(product.description_fr)||text(s.description_fr),
    description_en:text(product.description_en)||text(s.description_en),
    category:text(product.category)||text(s.category),
    brand:text(product.brand)||text(s.brand),
    ages:text(product.ages)||text(s.ages),
   };
   const saved=await fetch("/api/admin/products",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(patched)});
   const savedPayload=await saved.json() as {error?:string};if(!saved.ok)throw new Error(savedPayload.error||"Enregistrement impossible.");
   setAiNotice(`✓ ${text(product.name_fr)||text(s.name_fr)} : champs manquants complétés par l’IA.`);await reload();
  }catch(error){setAiNotice(error instanceof Error?`Erreur IA : ${error.message}`:"Erreur IA.");}finally{setAiBusy(null)}
 }
 async function applySelectedAction(){
  if(batchAction==="ai"){await repairBatch();return}
  const queue=shown.filter(({product})=>selected.has(String(product.id)));if(!queue.length)return;
  const numeric=batchAction==="price_set"||batchAction==="price_percent"||batchAction==="stock_set"||batchAction==="stock_add";const value=Number(batchValue.replace(",", "."));if(numeric&&(!Number.isFinite(value)||(batchAction==="stock_set"&&value<0))){window.alert("Entre une valeur valide.");return}
  const labels:Record<string,string>={hide:"Masquer partout",qc:"Afficher au Québec",conakry:"Afficher à Conakry",both:"Afficher dans les deux boutiques",price_set:`Fixer le prix à ${value}`,price_percent:`Modifier le prix de ${value}%`,stock_set:`Fixer le stock à ${Math.trunc(value)}`,stock_add:`Ajuster le stock de ${value>=0?"+":""}${Math.trunc(value)}`};
  if(!window.confirm(`${labels[batchAction]} pour ${queue.length} produit(s) sélectionné(s) ?`))return;
  setBatchBusy(true);setBatchProgress({done:0,total:queue.length});let ok=0,failed=0;
  for(const {product} of queue){try{let patched:Row={...product};if(batchAction==="price_set")patched[`price_${market}`]=Math.max(0,value);else if(batchAction==="price_percent")patched[`price_${market}`]=Math.max(0,Math.round(Number(product[`price_${market}`]||0)*(1+value/100)*100)/100);else if(batchAction==="stock_set")patched[`stock_${market}`]=Math.max(0,Math.trunc(value));else if(batchAction==="stock_add")patched[`stock_${market}`]=Math.max(0,Number(product[`stock_${market}`]||0)+Math.trunc(value));else{const visibility=batchAction;patched={...patched,visible_qc:visibility==="qc"||visibility==="both",visible_conakry:visibility==="conakry"||visibility==="both",visible:visibility!=="hide"}}const saved=await fetch("/api/admin/products",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(patched)});if(!saved.ok)throw new Error("Enregistrement impossible.");ok++}catch{failed++}finally{setBatchProgress(p=>({...p,done:p.done+1}))}}
  setBatchBusy(false);setSelected(new Set());setAiNotice(`✓ Action en lot terminée : ${ok} modifiée(s)${failed?`, ${failed} échec(s)`:""}.`);await reload();
 }
 async function repairBatch(){
  const queue=shown.filter(({product})=>selected.has(String(product.id))&&text(product.image_url));if(!queue.length)return;
  if(!window.confirm(`Corriger avec l’IA ${queue.length} fiche(s) sélectionnée(s) ?\n\nSeuls les champs manquants seront complétés. Les prix et stocks ne seront pas inventés.`))return;
  setBatchBusy(true);setAiNotice("");setBatchProgress({done:0,total:queue.length});let ok=0,failed=0;
  for(const {product} of queue){try{setAiBusy(String(product.id));const analyzed=await fetch("/api/admin/analyze-product",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({image_url:String(product.image_url)})});const payload=await analyzed.json() as {suggestion?:Record<string,unknown>;error?:string};if(!analyzed.ok||!payload.suggestion)throw new Error(payload.error||"Analyse IA impossible.");const s=payload.suggestion;const patched={...product,name_fr:text(product.name_fr)||text(s.name_fr),name_en:text(product.name_en)||text(s.name_en),description_fr:text(product.description_fr)||text(s.description_fr),description_en:text(product.description_en)||text(s.description_en),category:text(product.category)||text(s.category),brand:text(product.brand)||text(s.brand),ages:text(product.ages)||text(s.ages)};const saved=await fetch("/api/admin/products",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(patched)});if(!saved.ok)throw new Error("Enregistrement impossible.");ok++}catch{failed++}finally{setBatchProgress(p=>({...p,done:p.done+1}))}}
  setAiBusy(null);setBatchBusy(false);setSelected(new Set());setAiNotice(`✓ Correction IA en lot terminée : ${ok} corrigée(s)${failed?`, ${failed} échec(s)`:""}.`);await reload();
 }
 const problems=useMemo<Problem[]>(()=>products.map(product=>{
  const issues:string[]=[];
  const visible=Boolean(product[`visible_${market}`]);
  if(Number(product[`price_${market}`]||0)<=0)issues.push("Sans prix");
  if(!text(product.description_fr))issues.push("Description FR manquante");
  if(!text(product.description_en))issues.push("Description EN manquante");
  if(!text(product.image_url))issues.push("Sans photo");
  if(!text(product.name_fr))issues.push("Nom FR manquant");
  if(!text(product.name_en))issues.push("Nom EN manquant");
  if(!text(product.category))issues.push("Catégorie manquante");
  if(visible&&Number(product[`stock_${market}`]||0)<=0)issues.push("Visible mais sans stock");
  if(!visible)issues.push("Produit en attente");
  return issues.length?{product,issues,severity:visible&&(issues.includes("Sans prix")||issues.includes("Sans photo")||issues.includes("Nom FR manquant"))?"high":"medium"} as Problem:null;
 }).filter((x):x is Problem=>Boolean(x)),[products,market]);
 const brands=Array.from(new Set(problems.map(p=>text(p.product.brand)).filter(Boolean))).sort((a,b)=>a.localeCompare(b,"fr"));
 const categories=Array.from(new Set(problems.map(p=>text(p.product.category)).filter(Boolean))).sort((a,b)=>a.localeCompare(b,"fr"));
 const shown=problems.filter(p=>{const issueOk=filter==="all"||p.issues.some(i=>filter==="price"?i==="Sans prix":filter==="description"?i.includes("Description"):filter==="image"?i==="Sans photo":filter==="stock"?i.includes("stock"):filter==="pending"?i==="Produit en attente":i.includes("Nom"));const q=search.trim().toLocaleLowerCase("fr");const searchOk=!q||[p.product.name_fr,p.product.name_en,p.product.article_number,p.product.brand,p.product.category].some(v=>text(v).toLocaleLowerCase("fr").includes(q));const brandOk=brandFilter==="all"||text(p.product.brand)===brandFilter;const categoryOk=categoryFilter==="all"||text(p.product.category)===categoryFilter;const visible=Boolean(p.product[`visible_${market}`]);const visibilityOk=visibilityFilter==="all"||(visibilityFilter==="visible"?visible:!visible);return issueOk&&searchOk&&brandOk&&categoryOk&&visibilityOk});
 const counts={price:problems.filter(p=>p.issues.includes("Sans prix")).length,description:problems.filter(p=>p.issues.some(i=>i.includes("Description"))).length,image:problems.filter(p=>p.issues.includes("Sans photo")).length,stock:problems.filter(p=>p.issues.some(i=>i.includes("stock"))).length,pending:problems.filter(p=>p.issues.includes("Produit en attente")).length,name:problems.filter(p=>p.issues.some(i=>i.includes("Nom"))).length};
 return <section className="cms-panel cms-problems">{aiNotice&&<div className={aiNotice.startsWith("✓")?"cms-notice":"cms-error"} style={{marginBottom:12}}>{aiNotice}</div>}
  <div className="cms-panel-title"><div><h2>À vérifier · {markets[market].label}</h2><p>File de contrôle du catalogue : corrigez uniquement les fiches qui nécessitent une intervention.</p></div><strong>{problems.length} article{problems.length===1?"":"s"} à vérifier</strong></div>
  <div style={{display:"flex",gap:12,alignItems:"center",flexWrap:"wrap",margin:"14px 0 16px",padding:"12px 14px",border:"1px solid #c8d8e2",borderRadius:12,background:"#f8fbfd"}}><div style={{position:"relative",minWidth:340}}><span aria-hidden="true" style={{position:"absolute",left:12,top:"50%",transform:"translateY(-50%)",fontSize:17,color:"#365f78",pointerEvents:"none"}}>⌕</span><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Rechercher un article, une marque…" style={{width:"100%",boxSizing:"border-box",padding:"10px 36px 10px 38px",border:"1px solid #7898aa",borderRadius:10,background:"var(--cms-surface-soft)",color:"var(--cms-text)",fontWeight:600,outline:"none",boxShadow:"0 1px 3px rgba(18,52,77,.08)"}}/>{search&&<button type="button" aria-label="Effacer la recherche" onClick={()=>setSearch("")} style={{position:"absolute",right:7,top:"50%",transform:"translateY(-50%)",border:0,background:"transparent",cursor:"pointer",fontSize:18,color:"#496979"}}>×</button>}</div><select value={brandFilter} onChange={e=>setBrandFilter(e.target.value)} style={{padding:"10px 34px 10px 12px",border:"1px solid #7898aa",borderRadius:10,background:"var(--cms-surface-soft)",color:"var(--cms-text)",fontWeight:700,minWidth:190,cursor:"pointer"}}><option value="all">Toutes les marques</option>{brands.map(v=><option key={v} value={v}>{v}</option>)}</select><select value={categoryFilter} onChange={e=>setCategoryFilter(e.target.value)} style={{padding:"10px 34px 10px 12px",border:"1px solid #7898aa",borderRadius:10,background:"var(--cms-surface-soft)",color:"var(--cms-text)",fontWeight:700,minWidth:210,cursor:"pointer"}}><option value="all">Toutes les catégories</option>{categories.map(v=><option key={v} value={v}>{v}</option>)}</select><select value={visibilityFilter} onChange={e=>setVisibilityFilter(e.target.value as typeof visibilityFilter)} style={{padding:"10px 34px 10px 12px",border:"1px solid #7898aa",borderRadius:10,background:"var(--cms-surface-soft)",color:"var(--cms-text)",fontWeight:700,minWidth:205,cursor:"pointer"}}><option value="all">Toutes les visibilités</option><option value="visible">Visible sur le site</option><option value="hidden">Masqué / en attente</option></select><button className="cms-secondary" onClick={()=>{setSearch("");setBrandFilter("all");setCategoryFilter("all");setVisibilityFilter("all");setFilter("all")}} style={{padding:"10px 16px",border:"1px solid #7898aa",borderRadius:10,background:"#eef5f8",color:"#17384c",fontWeight:800,boxShadow:"0 1px 2px rgba(18,52,77,.08)"}}>↺ Réinitialiser</button><strong style={{marginLeft:"auto"}}>{shown.length} résultat(s)</strong></div>\n  <div className="cms-problem-stats"><button onClick={()=>setFilter("all")} className={filter==="all"?"active":""}><b>{problems.length}</b><span>Tous</span></button><button onClick={()=>setFilter("price")} className={filter==="price"?"active":""}><b>{counts.price}</b><span>Sans prix</span></button><button onClick={()=>setFilter("description")} className={filter==="description"?"active":""}><b>{counts.description}</b><span>Descriptions</span></button><button onClick={()=>setFilter("image")} className={filter==="image"?"active":""}><b>{counts.image}</b><span>Sans photo</span></button><button onClick={()=>setFilter("stock")} className={filter==="stock"?"active":""}><b>{counts.stock}</b><span>Stock</span></button><button onClick={()=>setFilter("name")} className={filter==="name"?"active":""}><b>{counts.name}</b><span>Noms</span></button><button onClick={()=>setFilter("pending")} className={filter==="pending"?"active":""}><b>{counts.pending}</b><span>En attente</span></button></div>
  <div style={{position:"fixed",top:86,left:218,right:24,zIndex:9991,display:"flex",gap:8,alignItems:"center",flexWrap:"wrap",padding:"10px 14px",border:"1px solid #b9cbd5",borderRadius:12,background:"var(--cms-surface)",color:"var(--cms-text)",boxShadow:"var(--cms-shadow)"}}><strong>À vérifier · {selected.size} sélectionné(s)</strong><button className="cms-secondary" disabled={batchBusy} onClick={()=>setSelected(new Set(shown.map(p=>String(p.product.id))))}>Tout sélectionner ({shown.length})</button><button className="cms-secondary" disabled={batchBusy||!selected.size} onClick={()=>setSelected(new Set())}>Désélectionner</button><select value={batchAction} disabled={batchBusy} onChange={e=>setBatchAction(e.target.value as typeof batchAction)} style={{minWidth:240,padding:"8px 10px",fontWeight:700}}><option value="ai">✦ IA · compléter les champs manquants</option><option value="hide">🔒 Masquer partout</option><option value="qc">🇨🇦 Afficher au Québec</option><option value="conakry">🇬🇳 Afficher à Conakry</option><option value="both">👁 Afficher dans les deux boutiques</option><option value="price_set">💲 Fixer le prix · boutique actuelle</option><option value="price_percent">％ Modifier le prix en % · boutique actuelle</option><option value="stock_set">📦 Fixer le stock · boutique actuelle</option><option value="stock_add">± Stock · ajouter / retirer</option></select>{(batchAction==="price_set"||batchAction==="price_percent"||batchAction==="stock_set"||batchAction==="stock_add")&&<input type="number" step={batchAction.includes("stock")?"1":"0.01"} value={batchValue} onChange={e=>setBatchValue(e.target.value)} placeholder={batchAction==="price_percent"?"ex. 10 ou -10":batchAction==="stock_add"?"ex. 5 ou -2":"Valeur"} style={{width:125,padding:"8px 10px"}}/>}<button className="cms-primary" disabled={batchBusy||!selected.size} onClick={()=>void applySelectedAction()}>{batchBusy?`⌛ ${batchProgress.done}/${batchProgress.total}`:"Appliquer à la sélection"}</button>{batchBusy&&<progress max={batchProgress.total||1} value={batchProgress.done} style={{width:160}}/>}</div><div style={{height:72}} aria-hidden="true"/>
  <div className="cms-problem-list" style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(320px,1fr))",gap:14,alignItems:"stretch"}}>{shown.map(({product,issues,severity})=><article key={String(product.id)} className={`cms-problem ${severity}`} style={{position:"relative",display:"grid",gridTemplateColumns:"110px minmax(0,1fr)",gridTemplateRows:"1fr auto",gap:14,padding:14,borderRadius:12,background:"var(--cms-surface)",color:"var(--cms-text)",border:"1px solid var(--cms-border)",minHeight:210,alignItems:"start"}}><label style={{position:"absolute",top:8,left:8,zIndex:2,background:"var(--cms-surface-raised)",color:"var(--cms-text)",padding:"5px 7px",borderRadius:7,fontWeight:700}}><input type="checkbox" checked={selected.has(String(product.id))} disabled={batchBusy} onChange={e=>setSelected(prev=>{const next=new Set(prev);e.target.checked?next.add(String(product.id)):next.delete(String(product.id));return next})}/> Sélectionner</label><div style={{width:110,height:110,display:"grid",placeItems:"center",background:"#fff",border:"1px solid #e2eaee",borderRadius:10,overflow:"hidden"}}>{product.image_url?<img src={String(product.image_url)} alt="" style={{maxWidth:"100%",maxHeight:"100%",width:"auto",height:"auto",objectFit:"contain"}}/>:<span className="cms-problem-noimage">?</span>}</div><div className="cms-problem-body"><small>{String(product.article_number||"Sans no d’article")}</small><h3>{String(product.name_fr||"Produit sans nom")}</h3><div className="cms-problem-tags">{issues.map(issue=><span key={issue}>{issue}</span>)}</div><p>{Number(product[`price_${market}`]||0)>0?marketPrice(product[`price_${market}`],market):"Prix non défini"} · {Boolean(product[`visible_${market}`])?"Visible sur le site":"En attente / masqué"}</p></div><div style={{gridColumn:"1 / -1",display:"grid",gridTemplateColumns:"1fr 1fr",gap:8}}><button className="cms-secondary" disabled={aiBusy!==null||!text(product.image_url)} title={!text(product.image_url)?"Une photo est nécessaire pour la correction IA":undefined} onClick={()=>void repairWithAi(product)}>{aiBusy===String(product.id)?"⌛ Correction IA…":"✦ Corriger avec l’IA"}</button><button className="cms-primary" onClick={()=>onEdit(product)}>Corriger la fiche</button></div></article>)}</div>
  {!shown.length&&<p className="cms-empty">Aucun problème dans cette catégorie.</p>}
 </section>
}
