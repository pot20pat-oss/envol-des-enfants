name: Refactor storefront presentation

on:
  push:
    branches:
      - refactor/storefront-presentation

permissions:
  contents: write

jobs:
  transform:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/storefront-presentation
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/refactor-storefront-presentation.mjs
      - name: Commit transformed storefront
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/storefront/storefront-page.tsx
          git commit -m "Raccorder les composants de présentation storefront"
          git push origin HEAD:refactor/storefront-presentation
